// solution #1
const solution = (message) => message.length * 2;

// solution #2
// const solution = (message) => Array.from(message).length * 2;

// solution #3
// const solution = (message) =>  message.split('').length * 2;